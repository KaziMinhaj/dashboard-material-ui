import { Typography } from "@mui/material";
import React from "react";

function RightSideBar() {
  return (
    <>
      <Typography variant="h6">Your selected exams(0)</Typography>
      <Typography variant="body2">Your take add/change later</Typography>
    </>
  );
}

export default RightSideBar;
